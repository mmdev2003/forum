services:
  forum-admin-unit-test:
    container_name: forum-admin-unit-test
    build:
      context: "../../../"
      dockerfile: ".github/test/unit/Dockerfile"
    depends_on:
      - forum-admin-db-unit-test
    networks:
      - net

  forum-admin-db-unit-test:
    image: postgres:latest
    container_name: forum-admin-db-unit-test
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "10003:5432"
    restart: unless-stopped
    networks:
      - net

networks:
  net: