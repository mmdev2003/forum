services:
  forum-status-unit-test:
    container_name: forum-status-unit-test
    build:
      context: "../../../"
      dockerfile: ".github/test/unit/Dockerfile"
    depends_on:
      - forum-status-db-unit-test
    networks:
      - net

  forum-status-db-unit-test:
    image: postgres:latest
    container_name: forum-status-db-unit-test
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "10003:5432"
    restart: unless-stopped
    networks:
      - net

networks:
  net: